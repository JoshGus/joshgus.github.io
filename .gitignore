.venv
.env
.DS_Store
photos-original/
data/photos.json
data/embeddings.json

# reverse-engineering artifacts (do not commit)
*.ipa

# Worker dependencies and local wrangler state (miniflare SQLite, cached
# account info) — regenerated by `npm install` / `wrangler dev`, never source.
node_modules/
.wrangler/
